import * as vscode from 'vscode';

// Локализация UI через официальный механизм l10n. Язык берётся из vscode.env.language;
// строки-исходники — английские, переводы (старт: en + ru) подключим бандлами в Фазе 3.
// Реэкспорт даёт короткий вызов t('...') по всему коду вместо vscode.l10n.t.
export const t = vscode.l10n.t;
