# Нормализуем окончания строк к LF — одинаково на Windows и Linux.
# Настройка живёт в репозитории и не зависит от core.autocrlf конкретной машины.
# Для JS/JSON/MD проекта LF везде — то, что нужно (Node, VS Code, git с ним работают без проблем).
* text=auto eol=lf
